import { makeStyles } from "@material-ui/styles";

export default makeStyles(() => ({
  container: {
    minHeight: "80px",
    marginTop: "auto",
  },
  image: {
    height: "80px",
    margin: "auto",
    padding: "2px",
  },
}));
